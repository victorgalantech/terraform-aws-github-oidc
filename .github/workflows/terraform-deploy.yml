name: Terraform Deploy - Bootstrap

on:
  push:
    branches: [main, develop, "feature/*", "release/*"]
    paths:
      - 'bootstrap/**'
      - '.github/workflows/terraform-deploy.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'bootstrap/**'
      - '.github/workflows/terraform-deploy.yml'
  schedule:
    - cron: '0 6 * * 1'  # Every Monday 06:00 UTC - weekly drift detection
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        type: choice
        options: [dev, qa, prod]
      action:
        description: 'Action to perform'
        required: true
        type: choice
        options: [plan, apply]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions: {}

env:
  AWS_REGION: 'eu-west-1'
  WORKING_DIR: './bootstrap'

jobs:
  # ============================================================
  # Job 1: Detect Environment
  # ============================================================
  detect-environment:
    name: Detect Environment
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      environment: ${{ steps.detect.outputs.environment }}
      env_upper:   ${{ steps.detect.outputs.env_upper }}
    steps:
      - id: detect
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            ENV="${{ github.event.inputs.environment }}"
          elif [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            ENV="prod"
          elif [[ "${{ github.ref }}" == refs/heads/release/* ]]; then
            ENV="qa"
          else
            ENV="dev"
          fi
          echo "environment=${ENV}" >> $GITHUB_OUTPUT
          echo "env_upper=$(echo ${ENV} | tr '[:lower:]' '[:upper:]')" >> $GITHUB_OUTPUT

  # ============================================================
  # Job 2: Validate & Lint
  # ============================================================
  validate:
    name: Validate & Lint
    runs-on: ubuntu-latest
    needs: detect-environment
    if: github.event_name != 'schedule'
    timeout-minutes: 15
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Read Terraform version
        id: tf-version
        run: echo "version=$(cat ${{ env.WORKING_DIR }}/.terraform-version | tr -d '[:space:]')" >> $GITHUB_OUTPUT

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ steps.tf-version.outputs.version }}

      - name: Terraform Format Check
        id: fmt
        run: terraform fmt -check -recursive
        working-directory: ${{ env.WORKING_DIR }}
        continue-on-error: true

      - name: Terraform Init (no backend)
        run: terraform init -backend=false
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Validate
        id: validate
        run: terraform validate -no-color
        working-directory: ${{ env.WORKING_DIR }}

      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: latest

      - name: Run TFLint
        id: tflint
        run: |
          tflint --init
          tflint --format compact
        working-directory: ${{ env.WORKING_DIR }}
        continue-on-error: true

      - name: Comment PR - Validation Results
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `#### 🎨 Terraform Format: \`${{ steps.fmt.outcome }}\`
            #### ✅ Terraform Validation: \`${{ steps.validate.outcome }}\`
            #### 🔍 TFLint: \`${{ steps.tflint.outcome }}\`

            *Pusher: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`
            })

  # ============================================================
  # Job 3: Security Scan
  # ============================================================
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: validate
    if: github.event_name != 'schedule'
    timeout-minutes: 15
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Run Checkov
        id: checkov
        uses: bridgecrewio/checkov-action@master
        with:
          directory: ${{ env.WORKING_DIR }}
          framework: terraform
          output_format: cli
          soft_fail: true
          quiet: false

      - name: Run tfsec
        id: tfsec
        uses: aquasecurity/tfsec-action@v1.0.3
        with:
          working_directory: ${{ env.WORKING_DIR }}
          soft_fail: true

      - name: Comment PR - Security Results
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `#### 🔒 Security Scan Results

            - **Checkov**: \`${{ steps.checkov.outcome }}\`
            - **tfsec**: \`${{ steps.tfsec.outcome }}\`

            *Environment: \`${{ needs.detect-environment.outputs.environment }}\`*`
            })

  # ============================================================
  # Job 4: Terraform Plan
  # ============================================================
  plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    needs: [detect-environment, validate, security-scan]
    if: github.event_name != 'schedule'
    timeout-minutes: 20
    permissions:
      id-token: write
      contents: read
      pull-requests: write
    env:
      ENV: ${{ needs.detect-environment.outputs.environment }}
    outputs:
      exitcode: ${{ steps.plan.outputs.exitcode }}
    steps:
      - uses: actions/checkout@v4

      - name: Read Terraform version
        id: tf-version
        run: echo "version=$(cat ${{ env.WORKING_DIR }}/.terraform-version | tr -d '[:space:]')" >> $GITHUB_OUTPUT

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ steps.tf-version.outputs.version }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars[format('AWS_ROLE_ARN_{0}', needs.detect-environment.outputs.env_upper)] }}
          role-session-name: TerraformPlan-${{ github.run_id }}-${{ env.ENV }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-tags: |
            Project=bootstrap
            environment=${{ env.ENV }}
            github-org=${{ github.repository_owner }}
            github-actor=${{ github.actor }}
            managed-by=github-actions
            action=terraform-plan

      - name: Generate backend config
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query "Account" --output text)
          BUCKET="${{ vars.COMPANY_NAME }}-tfstate-${{ env.ENV }}-${ACCOUNT_ID}"
          echo "Using bucket: ${BUCKET}"
          cat > backend-config.hcl <<EOF
          bucket       = "${BUCKET}"
          key          = "bootstrap/terraform.tfstate"
          region       = "${{ env.AWS_REGION }}"
          use_lockfile = true
          encrypt      = true
          EOF
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Init
        run: terraform init -backend-config=backend-config.hcl -input=false -reconfigure
        working-directory: ${{ env.WORKING_DIR }}

      - name: Generate terraform.tfvars
        run: |
          cat > terraform.tfvars <<EOF
          aws_region                = "${{ env.AWS_REGION }}"
          environment               = "${{ env.ENV }}"
          company_name              = "${{ vars.COMPANY_NAME }}"
          github_org                = "${{ vars.COMPANY_NAME }}"
          github_repo               = "*"
          enable_branch_restriction = true
          allowed_branches          = ["main", "develop", "release/*", "feature/*"]
          enable_cloudtrail         = false
          cloudtrail_retention_days = 1
          tags = {
            ManagedBy   = "Terraform"
            Team        = "DevOps"
            Project     = "bootstrap"
            environment = "${{ env.ENV }}"
          }
          EOF
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan \
            -out=tfplan-${{ env.ENV }}.binary \
            -detailed-exitcode \
            -no-color
        working-directory: ${{ env.WORKING_DIR }}

      - name: Show plan output
        if: steps.plan.outputs.exitcode == '2'
        run: terraform show -no-color tfplan-${{ env.ENV }}.binary > /tmp/plan-output.txt
        working-directory: ${{ env.WORKING_DIR }}

      - name: Upload plan artifact
        if: steps.plan.outputs.exitcode == '2'
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ env.ENV }}
          path: ${{ env.WORKING_DIR }}/tfplan-${{ env.ENV }}.binary
          retention-days: 5

      - name: Comment PR - Plan Results
        if: github.event_name == 'pull_request' && steps.plan.outputs.exitcode == '2'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('/tmp/plan-output.txt', 'utf8');
            const truncated = output.length > 60000 ? output.slice(-60000) : output;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `<details><summary>📋 Terraform Plan — ${{ env.ENV }} (click to expand)</summary>\n\n\`\`\`hcl\n${truncated}\n\`\`\`\n\n</details>\n\n*Run: [${{ github.run_id }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})*`
            })

      - name: Fail on plan error
        if: steps.plan.outputs.exitcode == '1'
        run: exit 1

  # ============================================================
  # Job 5: Terraform Apply
  # ============================================================
  apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: [detect-environment, plan]
    if: needs.plan.outputs.exitcode == '2'
    timeout-minutes: 30
    permissions:
      id-token: write
      contents: read
      pull-requests: write
    env:
      ENV: ${{ needs.detect-environment.outputs.environment }}
    steps:
      - uses: actions/checkout@v4

      - name: Read Terraform version
        id: tf-version
        run: echo "version=$(cat ${{ env.WORKING_DIR }}/.terraform-version | tr -d '[:space:]')" >> $GITHUB_OUTPUT

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ steps.tf-version.outputs.version }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars[format('AWS_ROLE_ARN_{0}', needs.detect-environment.outputs.env_upper)] }}
          role-session-name: TerraformApply-${{ github.run_id }}-${{ env.ENV }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-tags: |
            Project=bootstrap
            environment=${{ env.ENV }}
            github-org=${{ github.repository_owner }}
            github-actor=${{ github.actor }}
            managed-by=github-actions
            action=terraform-apply

      - name: Generate backend config
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query "Account" --output text)
          BUCKET="${{ vars.COMPANY_NAME }}-tfstate-${{ env.ENV }}-${ACCOUNT_ID}"
          echo "Using bucket: ${BUCKET}"
          cat > backend-config.hcl <<EOF
          bucket       = "${BUCKET}"
          key          = "bootstrap/terraform.tfstate"
          region       = "${{ env.AWS_REGION }}"
          use_lockfile = true
          encrypt      = true
          EOF
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Init
        run: terraform init -backend-config=backend-config.hcl -input=false -reconfigure
        working-directory: ${{ env.WORKING_DIR }}

      - name: Download plan artifact
        uses: actions/download-artifact@v4
        with:
          name: tfplan-${{ env.ENV }}
          path: ${{ env.WORKING_DIR }}

      - name: Terraform Apply
        id: apply
        run: terraform apply -auto-approve tfplan-${{ env.ENV }}.binary
        working-directory: ${{ env.WORKING_DIR }}

      - name: Upload outputs
        if: success()
        run: terraform output -json > outputs.json
        working-directory: ${{ env.WORKING_DIR }}

      - name: Upload outputs artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: terraform-outputs-${{ env.ENV }}
          path: ${{ env.WORKING_DIR }}/outputs.json
          retention-days: 30

      - name: Comment PR - Deployment Success
        if: success() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `#### ✅ Deployment Successful - ${{ env.ENV }}

            **Run ID:** \`${{ github.run_id }}\` &nbsp; **Actor:** @${{ github.actor }}`
            })

  # ============================================================
  # Job 6: Drift Detection (runs every Monday 06:00 UTC)
  # ============================================================
  drift-detection:
    name: Drift Detection (${{ matrix.environment }})
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    timeout-minutes: 20
    permissions:
      id-token: write
      contents: read
      issues: write
    strategy:
      fail-fast: false
      matrix:
        environment: [dev, qa, prod]
    steps:
      - uses: actions/checkout@v4

      - name: Prepare env
        id: prep
        run: echo "ENV_UPPER=$(echo ${{ matrix.environment }} | tr '[:lower:]' '[:upper:]')" >> $GITHUB_OUTPUT

      - name: Read Terraform version
        id: tf-version
        run: echo "version=$(cat ${{ env.WORKING_DIR }}/.terraform-version | tr -d '[:space:]')" >> $GITHUB_OUTPUT

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ steps.tf-version.outputs.version }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars[format('AWS_ROLE_ARN_{0}', steps.prep.outputs.ENV_UPPER)] }}
          role-session-name: DriftDetection-${{ github.run_id }}-${{ matrix.environment }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-tags: |
            Project=bootstrap
            environment=${{ matrix.environment }}
            managed-by=github-actions
            action=drift-detection

      - name: Generate backend config
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query "Account" --output text)
          BUCKET="${{ vars.COMPANY_NAME }}-tfstate-${{ matrix.environment }}-${ACCOUNT_ID}"
          echo "Using bucket: ${BUCKET}"
          cat > backend-config.hcl <<EOF
          bucket       = "${BUCKET}"
          key          = "bootstrap/terraform.tfstate"
          region       = "${{ env.AWS_REGION }}"
          use_lockfile = true
          encrypt      = true
          EOF
        working-directory: ${{ env.WORKING_DIR }}

      - name: Generate terraform.tfvars
        run: |
          cat > terraform.tfvars <<EOF
          aws_region                = "${{ env.AWS_REGION }}"
          environment               = "${{ matrix.environment }}"
          company_name              = "${{ vars.COMPANY_NAME }}"
          github_org                = "${{ vars.COMPANY_NAME }}"
          github_repo               = "*"
          enable_branch_restriction = true
          allowed_branches          = ["main", "develop", "release/*", "feature/*"]
          enable_cloudtrail         = false
          cloudtrail_retention_days = 1
          tags = {
            ManagedBy   = "Terraform"
            Team        = "DevOps"
            Project     = "bootstrap"
            environment = "${{ matrix.environment }}"
          }
          EOF
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Init
        run: terraform init -backend-config=backend-config.hcl -input=false -reconfigure
        working-directory: ${{ env.WORKING_DIR }}

      - name: Detect Drift
        id: drift
        run: |
          terraform plan \
            -detailed-exitcode \
            -no-color
        working-directory: ${{ env.WORKING_DIR }}
        continue-on-error: true

      - name: Create issue if drift detected
        if: steps.drift.outputs.exitcode == '2'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `⚠️ Infrastructure Drift Detected - ${{ matrix.environment }}`,
              body: `Drift detected in **${{ matrix.environment }}** environment.

            The real AWS state no longer matches the Terraform configuration.

            **Run:** [${{ github.run_id }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
            **Date:** ${new Date().toUTCString()}

            Please review and apply changes to reconcile infrastructure.`,
              labels: ['drift', 'infrastructure', '${{ matrix.environment }}']
            })